<div class="container">
  <h1>Flats</h1>
  <% @flats.each do |flat| %>
    <div class="bg-primary p-3 text-white m-3">
      <%= image_tag flat["imageUrl"], class: "w-75" %>
      <h5>Visit <%= flat["name"] %></h5>
      <p>Cost: EUR<%= flat["price"] %></p>
    </div>
  <% end %>
</div>
